export const cartSelector = (state) => state.cart.cartItems;
export const checkoutSelector = (state) => state.cart.checkoutProducts;
export const shippingSelector = (state) => state.shipping.shipping;
export const voucherSelector = (state) => state.voucher.voucher;
